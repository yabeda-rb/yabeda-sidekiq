# frozen_string_literal: true

RSpec.describe Evil::Metrics::Sidekiq do
  it "has a version number" do
    expect(Evil::Metrics::Sidekiq::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
